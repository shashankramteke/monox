apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: monoxai-dashboard
  namespace: monoxai
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: monoxai-dashboard
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 75
